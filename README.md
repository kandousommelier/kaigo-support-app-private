# 伴走支援ナビ

伴走支援ナビは、介護事業所向けに「今やること」「提出・回答リンク」「資料・動画」「成果物」「FAQ」「用語集」「利用上の注意」を確認できるGitHub Pages向けの静的Webアプリです。

本アプリは、案内と導線を提供するためのページです。回答データ、個人情報、個別ケースの詳細、施設内部の機密情報は保持しません。実データや提出物は、Googleフォーム、Googleドライブ、その他の外部リンク先で管理してください。

## 公開方法

ビルドは不要です。`index.html`、`assets/`、`data/`、`README.md`、`施設向け案内文.md`、`.nojekyll` をリポジトリ直下に置いた状態でGitHub Pagesを有効化すれば公開できます。

`index.html`、`assets/`、`data/` は相対パスでつながっているため、GitHub Pagesのリポジトリ配下URLでも動作します。ファイルを直接ダブルクリックして開いた場合は、ブラウザの制限でJSONを読み込めないことがあります。

## GitHub Pagesでの公開手順

1. GitHubのリポジトリ画面で `Settings` を開きます。
2. 左側メニューの `Pages` を開きます。
3. `Build and deployment` の `Source` で `Deploy from a branch` を選びます。
4. `Branch` で `main`、フォルダで `/root` を選び、`Save` を押します。
5. 数十秒から数分待つと、同じ `Pages` 画面に公開URLが表示されます。
6. 公開URLを開き、伴走支援ナビの入室画面が表示されることを確認します。
7. 公開URLを `施設向け案内文.md` の `伴走支援ナビURL` に記載し、施設へ案内します。

## 公開後の初回テスト手順

1. 公開URLを開き、入室画面に「伴走支援ナビ」と表示されることを確認します。
2. 共通ID `banso`、共通パスワード `support2026`、施設コード `shiga01` を入力し、施設別画面が表示されることを確認します。
3. 「今やること」「提出・回答リンク」「資料・動画」「成果物」「FAQ」「用語集」「利用上の注意」が表示されることを確認します。
4. URL未設定のリンクが「準備中」と表示されることを確認します。
5. `nagano01` や `kawasaki01` でも施設名や自治体名が切り替わることを確認します。
6. 非表示の `reserve01` では入室できないことを確認します。
7. 誤ったIDまたはパスワードでエラーメッセージが出ることを確認します。
8. スマホでカードが1列で読みやすく、ボタンが押しやすいことを確認します。
9. 個人情報を入力しない注意が表示されることを確認します。

## 入室情報

```text
共通ID: banso
共通パスワード: support2026
```

これは本格的な認証ではありません。関係者向けページであることと、情報管理に注意する必要があることを認識してもらうための入室作法です。公開されても支障のない情報だけを掲載してください。

## 施設コード一覧

初期登録されている施設コードは以下です。リザーブ施設は初期状態では非表示です。

| 施設コード | 自治体 | 初期表示 |
| --- | --- | --- |
| `shiga01` | 滋賀県 | 表示 |
| `shiga02` | 滋賀県 | 表示 |
| `shiga03` | 滋賀県 | 表示 |
| `shiga04` | 滋賀県 | 表示 |
| `shiga05` | 滋賀県 | 表示 |
| `nagano01` | 長野県 | 表示 |
| `nagano02` | 長野県 | 表示 |
| `nagano03` | 長野県 | 表示 |
| `nagano04` | 長野県 | 表示 |
| `matsumoto01` | 松本市 | 表示 |
| `matsumoto02` | 松本市 | 表示 |
| `matsumoto03` | 松本市 | 表示 |
| `matsuyama01` | 松山市 | 表示 |
| `matsuyama02` | 松山市 | 表示 |
| `matsuyama03` | 松山市 | 表示 |
| `kawasaki01` | 川崎市 | 表示 |
| `kawasaki02` | 川崎市 | 表示 |
| `reserve01` | その他リザーブ | 非表示 |
| `reserve02` | その他リザーブ | 非表示 |
| `reserve03` | その他リザーブ | 非表示 |
| `reserve04` | その他リザーブ | 非表示 |
| `reserve05` | その他リザーブ | 非表示 |
| `reserve06` | その他リザーブ | 非表示 |
| `reserve07` | その他リザーブ | 非表示 |
| `reserve08` | その他リザーブ | 非表示 |
| `reserve09` | その他リザーブ | 非表示 |
| `reserve10` | その他リザーブ | 非表示 |

## マスターデータの編集方法

表示内容は `data` 配下のJSONファイルで管理します。アプリ本体を修正しなくても、JSONを編集すれば施設名、提出・回答リンク、FAQ、成果物などを更新できます。

```text
data/config.json          共通ID、共通パスワード、基本注意文
data/municipalities.json  自治体マスター
data/facilities.json      施設名、施設コード、サービス種別、現在ステップ、今やること
data/steps.json           ステップマスター
data/links.json           提出・回答リンク、よく使うリンク、AI活用リンク
data/contents.json        資料・動画
data/deliverables.json    成果物名、目的、使用タイミング、テンプレートURL、記入例URL
data/faq.json             FAQのカテゴリ、質問、回答
data/terms.json           用語集
data/notices.json         利用上の注意
```

よくある更新は以下です。

- 施設名を変える: `data/facilities.json` の `facilityName` を編集します。
- 施設ごとの「今やること」を変える: `data/facilities.json` の `nextAction` を編集します。
- 提出リンクを追加・変更する: `data/links.json` の `url`、`title`、`description` を編集します。
- URL未確定のリンクを準備中にする: `data/links.json` の `url` を空文字にします。
- FAQを追加・変更する: `data/faq.json` を編集します。
- 成果物テンプレートを追加・変更する: `data/deliverables.json` の `templateUrl` や `sampleUrl` を編集します。
- 資料や動画を追加・変更する: `data/contents.json` を編集します。

## 施設追加方法

1. `data/facilities.json` に施設を1件追加します。
2. `facilityCode` は半角英数字で一意にします。
3. `municipalityCode` は `data/municipalities.json` のコードに合わせます。
4. 施設に表示したい現在ステップを `currentStep` に設定します。
5. 公開する施設は `isVisible: true` にします。

## 載せてよい情報

- 伴走支援の一般的な案内
- 提出・回答フォームへのリンク
- 資料・動画へのリンク
- 成果物テンプレートへのリンク
- FAQ
- 用語集
- 情報管理上の注意
- 公開されても支障のないスケジュールや作業案内

## 載せてはいけない情報

- 利用者名、職員名、家族名など個人が特定される情報
- 個別ケースの詳細
- インタビュー全文
- 施設内部の生々しい課題
- 行政共有前の所感
- 回答データ
- 機密情報や公開されると支障のある情報

## 注意事項

伴走支援ナビは、GitHub Pagesで公開される前提の静的Webアプリです。共通ID、共通パスワード、施設コードはクライアント側で簡易チェックされます。認証システムではないため、アプリ内には公開されても支障のない情報だけを掲載してください。

ID・パスワード入力は、関係者向けページであることを認識してもらうための入室作法です。個人情報、施設固有の生々しい情報、回答データ、行政共有前の所感は伴走支援ナビ内に保持しないでください。
